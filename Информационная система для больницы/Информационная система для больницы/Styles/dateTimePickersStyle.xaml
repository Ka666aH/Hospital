<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit">

    <Style x:Name="dateTimePickersStyle1" TargetType="{x:Type xctk:DateTimePicker}">
        <Setter Property="Background" Value="{DynamicResource backgroundColor}"/>
        <Setter Property="Foreground" Value="{DynamicResource textColor}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource textColor}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Format" Value="Custom"/>
        <Setter Property="FormatString" Value="dd.MM.yyyy HH:mm"/>
        <Setter Property="TimeFormat" Value="Custom"/>
        <Setter Property="TimeFormatString" Value="HH:mm"/>
        
        <!--<Setter Property="" Value="{DynamicResource textColor}"/>-->
        <!--<Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type xctk:DateTimePicker}">
                    <Border CornerRadius="10">
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>-->
    </Style>
    
</ResourceDictionary>