<UserControl x:Class="Информационная_система_для_больницы.MVVM.Views.registrarMenuButtonsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Информационная_система_для_больницы.MVVM.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel x:Name="registrarMenu" Grid.Row="3" VerticalAlignment="Stretch" Margin="0 10 0 0">

        <RadioButton x:Name="registrarMenu1" Content="Что-то" Style="{StaticResource menuButtonsStyle}"/>
        <RadioButton x:Name="registrarMenu2" Content="Что-то" Style="{StaticResource menuButtonsStyle}"/>
        <RadioButton x:Name="registrarMenu3" Content="Что-то" Style="{StaticResource menuButtonsStyle}"/>
        <RadioButton x:Name="registrarMenu4" Content="Что-то" Style="{StaticResource menuButtonsStyle}"/>


    </StackPanel>
</UserControl>
